*HOW T0 INSTALL SERVER MYSQL IN TERMINAL
-sudo apt update
-sudo apt install mysql-server -> Press Y 
-sudo mysql_secure_installation -> mengatur dan menghapus password untuk user root serta menghapus database

*HOW CHECK STATUS SERVER MYSQL
sudo systemctl status mysql-> UNTUK CHECK APAKAH STATUS SERVER NYA SUDAH RUN
sudo systemctl start mysql-> UNTUK START SERVER MYSQL
sudo sytemctl stop mysql->UNTUK STOP SERVER MYSQL

*FIRST LOGIN PHPMYADMIN
-sudo mysql
type this:
ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'PasswordKamu123';
FLUSH PRIVILEGES;
EXIT;
